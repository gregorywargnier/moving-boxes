{% extends 'base.html.twig' %}

{% block title %}New Boxes{% endblock %}

{% block body %}
    <h1>Create new Boxes</h1>

    {{ include('boxes/_form.html.twig') }}

    <a href="{{ path('movings:app:read', {'id': moving}) }}">back to list</a>
{% endblock %}
